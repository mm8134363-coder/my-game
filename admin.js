let ws,authed=false;
function login(){ws=new WebSocket((location.protocol==="https:"?"wss://":"ws://")+location.host);ws.onopen=()=>ws.send(JSON.stringify({type:"admin",key:document.getElementById("key").value}));ws.onmessage=e=>{let m=JSON.parse(e.data);if(m.type==="admin:auth"){if(!m.ok)return alert("مفتاح غير صحيح");authed=true;login.hidden=true;panel.hidden=false;state.textContent="🟢 مدير متصل";render(m)}if(m.type==="admin:update")render(m)}}}
function render(m){online.textContent=m.users.length;roomCount.textContent=m.rooms.length;reportCount.textContent=m.reports.length;rooms.innerHTML=m.rooms.map(r=>`<div class="item">🎙️ ${esc(r.name)} — ${r.users} مستخدم</div>`).join("");users.innerHTML=m.users.map(u=>`<div class="item">👤 ${esc(u.name)} — ${u.room||"خارج الغرفة"} — ${u.status}</div>`).join("");reports.innerHTML=m.reports.map(r=>`<div class="item">🚨 ${esc(r.from)}: ${esc(r.text)} — <b>${r.status}</b> ${r.status!=="تمت المعالجة"?`<button onclick="resolve('${r.id}')">معالجة</button>`:""}</div>`).join("")}
function createRoom(){let n=prompt("اسم الغرفة");if(!n)return;let id=prompt("معرّف إنجليزي للغرفة (مثال fun1)",n.toLowerCase().replace(/[^a-z0-9]/g,"")||"room1");ws.send(JSON.stringify({type:"admin:createRoom",name:n,id}))}
function resolve(id){ws.send(JSON.stringify({type:"admin:resolve",id}))}
function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}